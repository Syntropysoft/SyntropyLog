/**
 * @file src/config.ts
 * @description Re-exports the SyntropyLogConfig type from config.schema.ts.
 * No runtime validation here — that is handled by config.validator.ts.
 */

export type { SyntropyLogConfig } from './config.schema';
